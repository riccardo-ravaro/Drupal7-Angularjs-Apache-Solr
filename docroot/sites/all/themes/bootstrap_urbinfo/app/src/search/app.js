(function () {
  'use strict';

  angular.module('urbinfo.search', [
    'urbinfo.common',
    'ngCookies',
    'leaflet-directive'
  ]);
  // .config(config)
  // .run(preventRouting);

  // config.$inject = ['$locationProvider'];

  // function config($locationProvider) {
  //   $locationProvider.html5Mode(true);
  // }

  // function preventRouting($location, $rootElement) {
  //   $rootElement.off('click');
  // }

}());
